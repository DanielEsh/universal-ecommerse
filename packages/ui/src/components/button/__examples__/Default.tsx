import { BaseButton } from '../Button'

const Default = () => {
  return (
    <div>
      <BaseButton className="button" variants="ghost">
        Button
      </BaseButton>
    </div>
  )
}

export default Default
