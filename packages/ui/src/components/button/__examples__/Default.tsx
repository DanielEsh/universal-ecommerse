import { BaseButton } from '../Button'

const Default = () => {
  return (
    <div>
      <BaseButton className="button" variant="ghost">
        Button
      </BaseButton>
    </div>
  )
}

export default Default
