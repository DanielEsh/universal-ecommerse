/**
 * @Example
 useKeypress(['ArrowLeft', 'ArrowRight'], (event) => {
    if (event.key === 'ArrowLeft') {
      moveLeft();
    } else {
      moveRight();
    }
  });
 */

export const useKeyPress = (keys, handler) => {
  //
}